{
  "name": "@vyntrio-os/frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Vyntrio OS web dashboard — toolchain initialized in Phase 2",
  "scripts": {
    "dev": "node -e \"console.error('Frontend dev server not configured yet — see docs/08_FRONTEND.md'); process.exit(1)\"",
    "build": "node -e \"console.error('Frontend build not configured yet — see docs/08_FRONTEND.md'); process.exit(1)\"",
    "lint": "node -e \"console.log('No ESLint config yet — Phase 2'); process.exit(0)\"",
    "test": "node -e \"console.log('No frontend tests yet — Phase 2'); process.exit(0)\""
  },
  "engines": {
    "node": ">=20"
  }
}
